import styled from 'styled-components/native';
import { View } from 'react-native';

// a divider for the left and right panels
const Section = styled.View`
  flex: 2;
  borderColor: #ddd;
  borderRightWidth: 5;
`;

export default Section;
